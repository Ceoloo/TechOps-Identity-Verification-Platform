# Shared AION platform: event contracts, telemetry, orchestration, health.
# The event contract (src/identity/aion_events.py) now binds to this package
# instead of vendoring a copy. See AUDIT.md, migration step 2.
# NOTE: aion-company-os is a separate repo; CI needs read access to install it
# (add a GH token secret if the repo is private).
aion-platform @ git+https://github.com/Ceoloo/aion-company-os.git@main#subdirectory=packages/aion_platform

# Core identity platform is stdlib-only for testability.
# PRODUCTION: use real AEAD + KMS:
# cryptography>=42.0    # AES-256-GCM FieldCipher implementation
# fastapi>=0.110        # provider webhook receiver
# requests>=2.31        # provider + telemetry HTTP
